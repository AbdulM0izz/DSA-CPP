#include <iostream>
#include <vector>
using namespace std;
int main(){
//Vector <DataType> Vector name; Syntax Of Vector
//Vector <DataType> Vector name (size); If size needed 
// v.size(); to check to length of thhe vector
 // v.resize(new size); to chnage the size or length of the vector
// v.capacity(); to check the capicity in the memory or memory allocation; capacity >= size of the vector;
//And Capacity increased in the memory by power of two;
//
// To add elements in vector v.pushback(element); and element will added from last element onwards;

// v.insert(position , element) to add elements on particular position, and it is related to position of first element . And to check the position of first element      v.begin(); and v.end(); to check where the vector ends. Expample.                      v.insert( v.begin()+2 , element);

// To delete the element v.popback() ; last element will be removed
// To removed from middle . v.erase (position);
//v.clear(); to reomve all the elements;





  




    return 0;
}